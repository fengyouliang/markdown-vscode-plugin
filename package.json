{
  "name": "md-auto-preview",
  "displayName": "Markdown Editor (Editor / Split / Preview)",
  "description": "在 VS Code 中将 Markdown 以 Custom Editor 打开：同一标签页内提供 Editor / Split / Preview 三态，并按工作区记住上次选择。",
  "version": "1.0.0",
  "publisher": "local",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCustomEditor:mdAutoPreview.markdownEditor"
  ],
  "main": "./dist/extension.js",
  "capabilities": {
    "untrustedWorkspaces": {
      "supported": true,
      "description": "该扩展仅在本地渲染 Markdown 并提供自定义编辑器 UI，不执行外部命令、不上传用户内容，可安全运行于受限工作区。"
    }
  },
  "contributes": {
    "customEditors": [
      {
        "viewType": "mdAutoPreview.markdownEditor",
        "displayName": "Markdown Editor (Editor / Split / Preview)",
        "selector": [
          {
            "filenamePattern": "*.md",
            "scheme": "file"
          },
          {
            "filenamePattern": "*.md",
            "scheme": "vscode-remote"
          },
          {
            "filenamePattern": "*.md",
            "scheme": "untitled"
          },
          {
            "filenamePattern": "*.markdown",
            "scheme": "file"
          },
          {
            "filenamePattern": "*.markdown",
            "scheme": "vscode-remote"
          },
          {
            "filenamePattern": "*.markdown",
            "scheme": "untitled"
          }
        ],
        "priority": "default"
      }
    ]
  },
  "scripts": {
    "compile": "tsc -p .",
    "watch": "tsc -watch -p .",
    "vscode:prepublish": "npm run compile",
    "vsix": "node -e \"const { spawnSync } = require('child_process'); const version = require('./package.json').version; const out = 'md-auto-preview-' + version + '.vsix'; const npmCmd = process.platform === 'win32' ? 'npm.cmd' : 'npm'; const args = ['--cache', './node_modules/.cache/npm', 'exec', '--yes', '--package', '@vscode/vsce', '--', 'vsce', 'package', '--allow-missing-repository', '--skip-license', '--readme-path', 'README.md', '--changelog-path', 'helloagents/CHANGELOG.md', '-o', out]; const r = spawnSync(npmCmd, args, { stdio: 'inherit' }); process.exit(typeof r.status === 'number' ? r.status : 1);\""
  },
  "dependencies": {
    "highlight.js": "^11.11.1",
    "katex": "^0.16.28",
    "markdown-it": "^14.1.0",
    "markdown-it-container": "^4.0.0",
    "markdown-it-emoji": "^3.0.0",
    "markdown-it-footnote": "^4.0.0",
    "markdown-it-task-lists": "^2.1.1",
    "markdown-it-texmath": "^1.0.0",
    "mermaid": "^10.9.0"
  },
  "devDependencies": {
    "@types/markdown-it": "^14.1.2",
    "@types/node": "^20.11.30",
    "@types/vscode": "^1.85.0",
    "typescript": "^5.6.3"
  }
}
